const { ZLoginSchema, ZAccountSchema, ZUpdateAccountSchema, } = require("./account.schema");
const { ZQuestionSchema } = require("./question.schema");
const { ZSubjectSchema } = require("./subject.schema");

module.exports = {
  ZLoginSchema,
  ZAccountSchema,
  ZSubjectSchema,
  ZQuestionSchema,
  ZUpdateAccountSchema,
}